<%= link_to 'Add user', new_product_path,  {:remote => true, 'data-toggle' =>  "modal", 'data-target' => '#modal-window', class: 'btn btn-primary btn-lg'}  %>
<div id="modal-window" class="modal hide fade" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content"></div>
  </div>
</div>
<head>
   <title>Multiple serach</title>
   <link rel="stylesheet" href="chosen.min.css">
   <script type="text/javascript" src="/vendor/jquery-3.6.0.min.js"></script>
   <script type="text/javascript" src="/vendor/chosen.jquery.min.js"></script>
</head>
<body>
   <form>
      <select id="vendor" >
         <option> Option</option>
         <option> Option</option>
         <option> Option</option>
         <option> Option</option>
      </select>
   </form>
   <script type="text/javascript">
      
   </script>

</body>

<br>
<h1>Product list</h1>
<%= form_with(scope: "product") do |f| %>
  <%= f.select :id, @products.map {|p| [p.name, p.id]} %>
  <%= f.submit%>
<% end %>

 <table id='orderTable' class='table table-striped table-sm'>
   <thead>
   	 <th>Name</th>
   	 <th>Description</th>
   	 <th>Price</th>
   </thead> 

   	<% @products.each do |product| %>
   	<tr>
   	  <td><%= product.name %></td>
   	  <td><%= product.description %></td>
   	  <td><%= product.price %></td>
   	  <td> 
   	  	<%= link_to "Edit", edit_product_path(product) %>
         <%= link_to "Delete", product, method: :delete, data: { confirm: "Are you sure you want to delete this produc?"}%>
   	  </td> 
   	</tr>
   	<%end%>
 </table>